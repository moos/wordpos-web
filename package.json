{
  "name": "wordpos-web",
  "version": "1.0.1",
  "description": "wordpos for the web/browser",
  "author": "Moos <mooster@42at.com>",
  "keywords": [
    "wordpos"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/moos/wordpos-web.git"
  },
  "engines": {
    "node": ">=6"
  },
  "files": [
    "dict",
    "dist",
    "samples"
  ],
  "scripts": {
    "prebuild": "cp -r samples docs/samples && npm run replace-version",
    "build": "node node_modules/wordpos/tools/makeJsonDict.js index data --no-test",
    "postbuild": "cp -r node_modules/wordpos/dist .",
    "prestart": "echo \n\n\nDon't forget to npm run build\n\n",
    "start": "http-server -a localhost -o samples",
    "prepare": "npm run build",
    "replace-version": "sed -i s/@VERSION@/%npm_package_version%/g docs/samples/**/* docs/samples/*.html"
  },
  "dependencies": {
    "wordpos": "^2.0.0"
  },
  "devDependencies": {},
  "license": "ISC"
}
